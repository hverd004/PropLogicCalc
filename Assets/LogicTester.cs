using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LogicTester : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*List<string> formula = new List<string> { "p", "&&", "(", "!", "q", "||", "r", ")" };

        var solver = new PropositionalLogicSolver(formula);
        solver.SolveTruthTable();  // This will print the truth table to the console
        var FIM = new FormulaInputManager();
        FIM.SolveFormula(formula);*/
    }
}
